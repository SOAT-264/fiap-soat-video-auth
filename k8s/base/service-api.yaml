apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: video-processor
spec:
  selector:
    app: auth-service
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: http
